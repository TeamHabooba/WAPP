@* CourseCreate.razor — admin form to create a new course *@
@page "/admin/courses/create"

<PageTitle>New Course — Admin</PageTitle>

<div class="page-container">
    @if (!Auth.IsAdmin)
    {
        <LoadingSpinner Label="Redirecting…" />
    }
    else
    {
        <header class="page-header">
            <div>
                <a href="/admin/courses" class="link-back">← Courses</a>
                <h1>New Course</h1>
            </div>
        </header>

        <AlertBox Message="@_error" Type="danger" />

        <div class="form-card">
            <EditForm Model="_model" FormName="admin-course-create" OnValidSubmit="HandleCreate" novalidate>
                <DataAnnotationsValidator />

                <div class="form-group">
                    <label>Title</label>
                    <InputText class="form-input" @bind-Value="_model.Title" placeholder="Course title" />
                    <ValidationMessage For="@(() => _model.Title)" class="form-error-msg" />
                </div>

                <div class="form-group">
                    <label>Description</label>
                    <InputTextArea class="form-input form-textarea" @bind-Value="_model.Description"
                                   placeholder="What will learners gain from this course?" rows="3" />
                    <ValidationMessage For="@(() => _model.Description)" class="form-error-msg" />
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Category</label>
                        <InputSelect class="form-input" @bind-Value="_model.Category">
                            <option value="">Select…</option>
                            <option value="Fundamentals">Fundamentals</option>
                            <option value="Tools">Tools</option>
                            <option value="CTF">CTF</option>
                        </InputSelect>
                        <ValidationMessage For="@(() => _model.Category)" class="form-error-msg" />
                    </div>

                    <div class="form-group">
                        <label>Difficulty</label>
                        <InputSelect class="form-input" @bind-Value="_model.Difficulty">
                            <option value="Beginner">Beginner</option>
                            <option value="Intermediate">Intermediate</option>
                            <option value="Advanced">Advanced</option>
                        </InputSelect>
                    </div>

                    <div class="form-group">
                        <label>Icon Emoji</label>
                        <InputText class="form-input" @bind-Value="_model.IconEmoji" placeholder="e.g. 🔐" />
                    </div>
                </div>

                <div class="form-group form-check">
                    <InputCheckbox @bind-Value="_model.IsPublished" id="published" />
                    <label for="published">Publish immediately</label>
                </div>

                <div class="form-actions">
                    <a href="/admin/courses" class="btn-secondary">Cancel</a>
                    <button type="submit" class="btn-primary" disabled="@_isSubmitting">
                        @(_isSubmitting ? "Saving…" : "Create Course")
                    </button>
                </div>
            </EditForm>
        </div>
    }
</div>
